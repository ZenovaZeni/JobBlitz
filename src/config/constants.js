// ── Pricing ──────────────────────────────────────────────────────────────────
export const PRO_PRICE = '$9.99'
export const PRO_PRICE_LABEL = `${PRO_PRICE}/mo`

export const PRO_FEATURES = [
  '50 tailoring sessions / mo',
  'AI cover letter generator',
  'Interview STAR prep',
  'PDF export + all templates',
  'Priority AI processing',
]

// ── Validation ────────────────────────────────────────────────────────────────
export const JD_MAX_CHARS = 15_000
